jQuery(document).ready(function($) {
    
    // Slick Banner Home
    $('.banner-carousel').slick({
        infinite: true,
        speed: 500,
        fade: true,
        cssEase: 'linear'
    });    
});